---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="About"
  description="Neighbour is Jared and Abigail — a husband-and-wife creative studio. We started this because we believe the best creative work happens when someone actually shows up for you."
>

  <!-- ========== HERO ========== -->
  <section class="about-hero film-grain">
    <div class="container about-hero__inner">
      <div class="about-hero__copy reveal">
        <span class="eyebrow">About us</span>
        <h1 class="text-balance">Hi. We&rsquo;re Jared and Abigail.</h1>
        <p class="lede">
          We started Neighbour for the people who show up for everyone else &mdash; and needed someone to show up for them.
        </p>
        <a href="/contact" class="btn btn--solid">Work with us &rarr;</a>
      </div>

      <div class="about-hero__visual reveal reveal--delay-2">
        <div class="about-hero__frame">
          <img
            src="/images/bts/bts-mountains-peace.jpg"
            alt="Jared on location in the mountains during a shoot"
            class="about-hero__img"
            loading="eager"
            onerror="this.style.display='none'"
          />
          <div class="about-hero__monitor-badge" aria-hidden="true">
            <span class="monitor-badge__dot"></span>
            On location
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== SHORT VERSION ========== -->
  <section class="section">
    <div class="container--narrow">
      <div class="section__head reveal">
        <span class="eyebrow">Short version</span>
        <h2 class="text-balance">The short version.</h2>
        <span class="bar"></span>
      </div>
      <div class="prose reveal reveal--delay-1">
        <p>Jared is a filmmaker and creative strategist who spent years in commercials and film learning how to tell a story that lands. Abigail is a brand strategist, photographer, and director who thinks about who a story is <em>for</em> before she thinks about how it looks.</p>
        <p>Together we built a studio around the work that matters most to us &mdash; the helpers. The ones doing something real in the world.</p>
        <p>When you hire us, you get us. Our full attention, start to finish. No account manager, no handoff, no stranger on the edit.</p>
      </div>
    </div>
  </section>

  <!-- ========== BEHIND THE LENS ========== -->
  <section class="section section--cream behind-lens">
    <div class="container">
      <div class="section__head reveal">
        <span class="eyebrow">The craft</span>
        <h2 class="text-balance">We go where the story is.</h2>
        <span class="bar"></span>
      </div>
      <div class="behind-lens__grid">
        <div class="behind-lens__card reveal reveal--delay-1">
          <div class="behind-lens__img-wrap">
            <img
              src="/images/bts/bts-mountains-snow.jpg"
              alt="Production crew hiking to a remote mountain location in winter conditions"
              class="behind-lens__img"
              loading="lazy"
              onerror="this.style.display='none'"
            />
          </div>
          <p class="behind-lens__caption">Rocky Mountains — documentary production</p>
        </div>
        <div class="behind-lens__card reveal reveal--delay-2">
          <div class="behind-lens__img-wrap">
            <img
              src="/images/bts/bts-wheelchair.jpg"
              alt="Camera monitor frame: athlete in wheelchair during documentary shoot"
              class="behind-lens__img"
              loading="lazy"
              onerror="this.style.display='none'"
            />
          </div>
          <p class="behind-lens__caption">Athlete portrait — mission-driven storytelling</p>
        </div>
        <div class="behind-lens__card reveal reveal--delay-3">
          <div class="behind-lens__img-wrap">
            <img
              src="/images/bts/bts-portrait.jpg"
              alt="Camera monitor frame: documentary subject in a community space"
              class="behind-lens__img"
              loading="lazy"
              onerror="this.style.display='none'"
            />
          </div>
          <p class="behind-lens__caption">Community documentary — portrait session</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== WHAT WE BELIEVE ========== -->
  <section class="section">
    <div class="container--narrow">
      <div class="section__head reveal">
        <span class="eyebrow">What we believe</span>
        <h2 class="text-balance">What we believe.</h2>
        <span class="bar"></span>
      </div>

      <div class="beliefs">
        <div class="belief reveal reveal--delay-1">
          <h3>We believe no one is born void.</h3>
          <p>Every person and every organization has something worth bringing to the surface. Our job is to help you find it.</p>
        </div>
        <div class="belief reveal reveal--delay-2">
          <h3>We believe in moving forward with honesty.</h3>
          <p>Upfront about costs, timelines, and fit. If something isn&rsquo;t serving you, we&rsquo;ll say so.</p>
        </div>
        <div class="belief reveal reveal--delay-3">
          <h3>We believe in the right thing for the right people.</h3>
          <p>Not the flashiest thing. The thing that actually works &mdash; for your audience, your work, and the moment you&rsquo;re in.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ========== HOW WE WORK ========== -->
  <section class="section section--cream">
    <div class="container--narrow">
      <div class="section__head reveal">
        <span class="eyebrow">How we work</span>
        <h2 class="text-balance">How we work.</h2>
        <span class="bar"></span>
      </div>
      <div class="prose reveal reveal--delay-1">
        <p>We take on a limited number of projects so we can be fully present for each one. We ask a lot of questions up front, bring our instincts, and welcome yours.</p>
        <p>A good neighbour isn&rsquo;t just someone who shows up. It&rsquo;s someone who stays.</p>
      </div>
    </div>
  </section>

  <!-- ========== CTA ========== -->
  <section class="section--lavender cta-band film-grain">
    <div class="container cta-band__inner reveal">
      <h2 class="text-balance">Think we might be neighbours?</h2>
      <div class="btn-group">
        <a href="/contact" class="btn btn--solid">Start a project</a>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  /* ---- Hero ---- */
  .about-hero {
    padding: clamp(6rem, 12vw, 9rem) 0 clamp(3rem, 6vw, 5rem);
    background:
      radial-gradient(ellipse at 30% 0%, rgba(234, 170, 66, 0.18) 0%, transparent 55%),
      radial-gradient(circle at 85% 80%, rgba(85, 156, 173, 0.10) 0%, transparent 45%),
      var(--paper);
    overflow: hidden;
  }

  .about-hero__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: clamp(2.5rem, 6vw, 5rem);
  }

  .about-hero__copy {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .about-hero__copy .eyebrow { margin-bottom: 0; }

  .about-hero__copy h1 {
    font-size: clamp(2.25rem, 5vw, 3.75rem);
    margin: 0;
  }

  .about-hero__copy .lede {
    font-size: clamp(1rem, 1.6vw, 1.1875rem);
    line-height: 1.65;
    color: var(--ink-soft);
    max-width: 44ch;
    margin: 0;
  }

  /* ---- Hero visual frame ---- */
  .about-hero__visual {
    position: relative;
  }

  .about-hero__frame {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
    aspect-ratio: 16 / 10;
    box-shadow: 0 24px 64px rgba(26, 26, 26, 0.18), 0 4px 16px rgba(26, 26, 26, 0.1);
    background: linear-gradient(135deg, rgba(234, 170, 66, 0.22) 0%, rgba(85, 156, 173, 0.14) 100%);
  }

  .about-hero__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    filter: contrast(1.05) saturate(0.9);
  }

  .about-hero__monitor-badge {
    position: absolute;
    bottom: 1rem;
    left: 1rem;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(26, 26, 26, 0.75);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border: 1px solid rgba(250, 249, 246, 0.15);
    border-radius: 999px;
    padding: 0.375rem 0.875rem 0.375rem 0.625rem;
    font-family: var(--font-sans);
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(250, 249, 246, 0.9);
  }

  .monitor-badge__dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--honey);
    animation: badge-pulse 2.4s ease-in-out infinite;
    flex-shrink: 0;
  }

  @keyframes badge-pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(0.8); }
  }

  @media (prefers-reduced-motion: reduce) {
    .monitor-badge__dot { animation: none; }
  }

  @media (max-width: 820px) {
    .about-hero__inner {
      grid-template-columns: 1fr;
    }
    .about-hero__visual { order: -1; }
    .about-hero__frame { aspect-ratio: 16 / 9; max-height: 340px; }
  }

  /* ---- Behind the lens ---- */
  .behind-lens__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  .behind-lens__card {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .behind-lens__img-wrap {
    width: 100%;
    aspect-ratio: 16 / 10;
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: linear-gradient(135deg, rgba(234, 170, 66, 0.14) 0%, rgba(85, 156, 173, 0.10) 100%);
    transition: transform var(--t-base) var(--ease), box-shadow var(--t-base) var(--ease);
  }

  .behind-lens__card:hover .behind-lens__img-wrap {
    transform: translateY(-3px);
    box-shadow: var(--shadow-lift);
  }

  .behind-lens__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    filter: contrast(1.04) saturate(0.88);
  }

  .behind-lens__caption {
    font-size: 0.8125rem;
    color: var(--ink-soft);
    line-height: 1.5;
    font-style: italic;
    margin: 0;
    padding-left: 0.25rem;
  }

  @media (max-width: 820px) {
    .behind-lens__grid { grid-template-columns: 1fr; gap: 2rem; }
    .behind-lens__img-wrap { aspect-ratio: 16 / 9; }
  }

  /* ---- Section heads ---- */
  .section__head { margin-bottom: 2.5rem; max-width: 720px; }
  .section__head .eyebrow { margin-bottom: 1rem; }
  .section__head h2 { margin-bottom: 1.25rem; }

  /* ---- Beliefs ---- */
  .beliefs { display: flex; flex-direction: column; }

  .belief {
    padding: 1.75rem 0;
    border-top: 1px solid var(--rule);
  }

  .belief:last-child { border-bottom: 1px solid var(--rule); }

  .belief h3 {
    font-size: 1.25rem;
    margin-bottom: 0.75rem;
    color: var(--lavender-deep);
  }

  .belief p { font-size: 1rem; line-height: 1.75; }

  /* ---- CTA band ---- */
  .cta-band {
    padding: clamp(5rem, 10vw, 8rem) 0;
    text-align: center;
    background:
      radial-gradient(circle at 80% 20%, rgba(234, 170, 66, 0.15), transparent 50%),
      var(--lavender-deep);
  }

  .cta-band__inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    max-width: 640px;
  }

  .cta-band h2 { color: var(--paper); font-size: clamp(1.875rem, 3.6vw, 2.75rem); }
</style>
