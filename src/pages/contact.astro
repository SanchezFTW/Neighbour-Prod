---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Contact"
  description="Tell us a bit about your project. We read every inquiry ourselves — usually within a day or two — and if we're a good fit, we'll set up a call."
>

  <!-- ========== HERO ========== -->
  <section class="contact-hero film-grain">
    <div class="container contact-hero__inner">
      <h1 class="reveal text-balance">Let&rsquo;s talk.</h1>
      <p class="lede reveal reveal--delay-1">
        You don&rsquo;t need a polished brief or a clear budget before you reach out. Just tell us what you&rsquo;re working on and where you feel stuck. We&rsquo;ll take it from there.
      </p>
      <p class="contact-hero__sub reveal reveal--delay-2">We read every message ourselves and respond within a few days.</p>
    </div>
  </section>

  <!-- ========== FORM ========== -->
  <section class="section contact-form-section">
    <div class="container contact-layout">
      <div class="contact-form-col">
        <div class="form-guidance reveal">
          <p class="form-guidance__label">A few things that help us show up prepared:</p>
          <ul class="form-guidance__list">
            <li>What&rsquo;s your organization and what do you do?</li>
            <li>What are you trying to make or figure out?</li>
            <li>Is there a timeline or deadline we should know about?</li>
          </ul>
        </div>

      <form
        id="inquiry-form"
        class="inquiry-form reveal reveal--delay-1"
        action="https://formspree.io/f/xwvadbkl"
        method="POST"
        novalidate
      >
        <input type="text" name="_gotcha" tabindex="-1" autocomplete="off" style="position:absolute;left:-10000px;width:1px;height:1px;opacity:0;" aria-hidden="true" />
        <div class="field-row">
          <div class="field">
            <label for="firstName">First name</label>
            <input id="firstName" name="firstName" type="text" required autocomplete="given-name" />
          </div>

          <div class="field">
            <label for="lastName">Last name</label>
            <input id="lastName" name="lastName" type="text" required autocomplete="family-name" />
          </div>
        </div>

        <div class="field">
          <label for="email">Your email</label>
          <input id="email" name="email" type="email" required autocomplete="email" />
        </div>

        <div class="field">
          <label for="org">Organization <span class="field__opt">(if any)</span></label>
          <input id="org" name="organization" type="text" autocomplete="organization" />
        </div>

        <div class="field">
          <label for="project">What are you working on?</label>
          <textarea id="project" name="project" rows="5" required placeholder="A short paragraph is plenty."></textarea>
        </div>

        <div class="field-row">
          <div class="field">
            <label for="projectType">Project type</label>
            <select id="projectType" name="projectType" required>
              <option value="" disabled selected>Pick one</option>
              <option value="Crafting a strategy">Crafting a strategy</option>
              <option value="Building a new brand">Building a new brand</option>
              <option value="Launching a campaign">Launching a campaign</option>
              <option value="Making a new website">Making a new website</option>
              <option value="Driving sales or traffic">Driving sales or traffic</option>
              <option value="Something not listed">Something not listed</option>
            </select>
          </div>

          <div class="field">
            <label for="timeline">Timeline</label>
            <select id="timeline" name="timeline" required>
              <option value="" disabled selected>Pick one</option>
              <option value="ASAP">ASAP</option>
              <option value="Next 1–3 months">Next 1&ndash;3 months</option>
              <option value="3–6 months">3&ndash;6 months</option>
              <option value="Just exploring">Just exploring</option>
            </select>
          </div>
        </div>

        <div class="field-row">
          <div class="field">
            <label for="budget">Budget range</label>
            <select id="budget" name="budget" required>
              <option value="" disabled selected>Pick one</option>
              <option value="Under $5k">Under $5k</option>
              <option value="$5k–$15k">$5k&ndash;$15k</option>
              <option value="$15k–$30k">$15k&ndash;$30k</option>
              <option value="$30k+">$30k+</option>
              <option value="Not sure yet">Not sure yet</option>
            </select>
          </div>

          <div class="field">
            <label for="referral">How&rsquo;d you find us?</label>
            <select id="referral" name="referral" required>
              <option value="" disabled selected>Pick one</option>
              <option value="Referral">Referral from a friend or colleague</option>
              <option value="Instagram">Instagram</option>
              <option value="Google search">Google search</option>
              <option value="Past project">Saw a past project of ours</option>
              <option value="Event">At an event</option>
              <option value="Other">Other</option>
            </select>
          </div>
        </div>

        <div class="form-actions">
          <button type="submit" class="btn btn--solid">Send it over</button>
          <p id="form-status" class="form-status" role="status" aria-live="polite"></p>
        </div>
      </form>
      </div>

      <aside class="next-steps reveal reveal--delay-1" aria-labelledby="next-heading">
        <h2 id="next-heading">What happens next</h2>
        <ol class="next-list">
          <li><span class="next-num">1</span><p>We read your inquiry (actually &mdash; it&rsquo;s just the two of us).</p></li>
          <li><span class="next-num">2</span><p>If we think we&rsquo;re a fit, we&rsquo;ll reach out to book a call within about a week.</p></li>
          <li><span class="next-num">3</span><p>If we&rsquo;re not the right fit, we&rsquo;ll tell you that too &mdash; and point you toward someone who might be.</p></li>
        </ol>

        <div class="not-sure">
          <p class="not-sure__heading">Not sure if we&rsquo;re the right fit?</p>
          <p class="not-sure__body">Reach out anyway. The worst that happens is we point you in the right direction.</p>
        </div>
      </aside>
    </div>
  </section>


</BaseLayout>

<style>
  .contact-hero {
    padding: clamp(7rem, 14vw, 10rem) 0 clamp(2rem, 4vw, 3rem);
    background:
      radial-gradient(ellipse at 50% 0%, rgba(234, 170, 66, 0.2) 0%, transparent 55%),
      var(--paper);
  }

  .contact-hero__inner {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    max-width: 780px;
  }

  .contact-hero__sub {
    font-size: 0.9375rem;
    color: var(--ink-soft);
    margin: 0;
    opacity: 0.85;
  }

  .contact-form-section { padding-top: clamp(2rem, 4vw, 3rem); }

  .contact-layout {
    display: grid;
    grid-template-columns: 1.4fr 1fr;
    gap: clamp(2.5rem, 5vw, 4rem);
    align-items: start;
  }

  .contact-form-col {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  @media (max-width: 860px) {
    .contact-layout { grid-template-columns: 1fr; }
  }

  .inquiry-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .field {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .field-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }

  @media (max-width: 520px) {
    .field-row { grid-template-columns: 1fr; }
  }

  .field label,
  .field legend {
    font-size: 0.8125rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--lavender-deep);
  }

  .field__opt {
    font-weight: 400;
    letter-spacing: 0.02em;
    text-transform: none;
    color: var(--ink-mute);
    font-size: 0.75rem;
  }

  .field input[type="text"],
  .field input[type="email"],
  .field textarea,
  .field select {
    font-family: inherit;
    font-size: 1rem;
    color: var(--ink);
    background: var(--paper);
    padding: 0.85rem 1rem;
    border: 1px solid var(--rule-strong);
    border-radius: var(--radius);
    transition: border-color var(--t-fast) ease, box-shadow var(--t-fast) ease;
    width: 100%;
  }

  .field textarea { resize: vertical; min-height: 140px; line-height: 1.6; }

  .field input:focus,
  .field textarea:focus,
  .field select:focus {
    outline: none;
    border-color: var(--honey);
    box-shadow: 0 0 0 3px rgba(234, 170, 66, 0.22);
  }

  .form-actions {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    flex-wrap: wrap;
    margin-top: 0.5rem;
  }

  .form-status {
    font-size: 0.875rem;
    color: var(--ink-soft);
    margin: 0;
  }

  .form-status.is-error { color: #a23a3a; }
  .form-status.is-success { color: var(--teal-deep); }

  /* What happens next */
  .next-steps {
    background: var(--paper-cream);
    border-radius: var(--radius-lg);
    padding: clamp(1.75rem, 3vw, 2.5rem);
    position: sticky;
    top: 7rem;
  }

  .next-steps h2 {
    font-size: 1.25rem;
    margin-bottom: 1.5rem;
  }

  .next-list {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .next-list li {
    display: grid;
    grid-template-columns: 36px 1fr;
    gap: 1rem;
    align-items: start;
  }

  .next-num {
    width: 32px;
    height: 32px;
    border-radius: 999px;
    background: var(--honey);
    color: var(--ink);
    font-family: var(--font-display);
    font-weight: 700;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 0.875rem;
  }

  .next-list p {
    font-size: 0.9375rem;
    line-height: 1.65;
    margin: 0;
  }

  /* Form guidance */
  .form-guidance {
    background: var(--paper-cream);
    border-left: 3px solid var(--honey);
    border-radius: var(--radius);
    padding: 1.25rem 1.5rem;
  }

  .form-guidance__label {
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--lavender-deep);
    letter-spacing: 0.04em;
    margin: 0 0 0.75rem;
  }

  .form-guidance__list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .form-guidance__list li {
    font-size: 0.9375rem;
    color: var(--ink-soft);
    line-height: 1.55;
    padding-left: 1.1rem;
    position: relative;
  }

  .form-guidance__list li::before {
    content: '–';
    position: absolute;
    left: 0;
    color: var(--honey-deep);
  }

  /* Not sure CTA */
  .not-sure {
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--rule);
  }

  .not-sure__heading {
    font-family: var(--font-display);
    font-size: 1rem;
    font-weight: 600;
    color: var(--lavender-deep);
    margin: 0 0 0.4rem;
  }

  .not-sure__body {
    font-size: 0.9375rem;
    color: var(--ink-soft);
    line-height: 1.6;
    margin: 0;
  }

  @media (max-width: 860px) {
    .next-steps { position: static; }
  }

  @media (max-width: 520px) {
    .field input[type="text"],
    .field input[type="email"],
    .field textarea,
    .field select {
      font-size: 16px; /* prevents iOS zoom on focus */
      padding: 0.85rem 0.9rem;
    }
    .form-actions { flex-direction: column; align-items: stretch; gap: 0.85rem; }
    .form-actions .btn { justify-content: center; }
  }
</style>

<script>
  function setupInquiryForm() {
    const form = document.getElementById('inquiry-form') as HTMLFormElement | null;
    const status = document.getElementById('form-status');
    if (!form || !status) return;
    if (form.dataset.fsBound === '1') return;
    form.dataset.fsBound = '1';

    const submitBtn = form.querySelector('button[type="submit"]') as HTMLButtonElement | null;

    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      if (!form.checkValidity()) {
        form.reportValidity();
        return;
      }

      status.classList.remove('is-error', 'is-success');
      status.textContent = 'Sending\u2026';
      if (submitBtn) submitBtn.disabled = true;

      const fd = new FormData(form);
      const firstName = String(fd.get('firstName') || '');
      const lastName = String(fd.get('lastName') || '');
      const fullName = `${firstName} ${lastName}`.trim();

      fd.set('_subject', `New inquiry from ${fullName}`);
      const emailVal = String(fd.get('email') || '');
      if (emailVal) fd.set('_replyto', emailVal);

      try {
        const res = await fetch(form.action, {
          method: 'POST',
          body: fd,
          headers: { Accept: 'application/json' },
        });

        if (res.ok) {
          status.classList.add('is-success');
          status.textContent = 'Thanks \u2014 we got your note and will be in touch soon.';
          form.reset();
        } else {
          const data = await res.json().catch(() => null);
          const msg = data?.errors?.map((err: { message: string }) => err.message).join(', ');
          throw new Error(msg || 'Submission failed');
        }
      } catch (err) {
        status.classList.add('is-error');
        status.textContent = 'Something went wrong. Please email info@neighbourprod.com directly.';
      } finally {
        if (submitBtn) submitBtn.disabled = false;
      }
    });
  }

  setupInquiryForm();
  document.addEventListener('astro:page-load', setupInquiryForm);
</script>
