# Project Template

Copy this folder into a client directory and rename `<project-slug>` to your project's slug.

Match the slug to the website content file in `website/src/content/projects/<slug>.md` when possible.

- `web/` — sized/compressed exports used on the site
- `source/` — originals, RAWs, deliverables, masters
